services:
  crm:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - crm_data:/app/data
      - ./credentials:/app/credentials:ro
    env_file:
      - .env
    restart: unless-stopped

volumes:
  crm_data:
